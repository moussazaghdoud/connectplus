{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '--- ENV CHECK ---' && echo DATABASE_URL=$DATABASE_URL | head -c 30 && echo '...' && echo NODE_ENV=$NODE_ENV && echo '--- MIGRATE ---' && npx prisma migrate deploy 2>&1 && echo '--- SERVER ---' && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 1
  }
}
